# Module 02 Devlog

## Proof of Completion

This is the first output from the code I created, this shows that everything displays correctly, we can compare this to the next screenshot to prove that all the functions work correctly.

![First output in the website before interaction](/tutorials/t02/reflections/img/before-int.png) 

**---------------------------------------------------------------------------------------------------------------------**

After clicking the buttons I had taken another screenshot here wich shows the level up, health and gold state objects.

![after updated image after button presses](/tutorials/t02/reflections/img/after-int.png)

**Extension task completed successfully:** [Yes]

## Concept Mapping

- **Concept 1:** [21] — [It is the process of creating a component manually, which was done using the example code.]
  - **Implementation:** [it was used several times throughout the code, first implimented in the greetings section as shown.  (
    
    function Greeting() {
    const name = "Students";
    const topic = "Full-Stack";

    return (
        \<div className="welcome-banner">
            \<h1>Hello {name}!</h1>
            \<p>Welcome to {topic}.</p>
        \</div>
    );
}

     export default Greeting;)]


## Analysis and Implications



**Reflection:**  
[ If I were to use AI I would have likely completed the content much sooner, in future I will definately use AI to do the extension as while working through taks 5 I had many difficulties in setting up and running the code in a different context than I originally did with the first parts. I particuallry had an issue with knowing if I was suppossed to name the things the way IO did eg - in playerStats I didn't know if I should've even set it as that name, and I didn't know if I could set it all up in the ways that I did and it still be correct. ]
